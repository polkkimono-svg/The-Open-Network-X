[package]
name = "onx-genesis"
version = "0.1.0"
edition = "2021"
description = "Deterministic ONX genesis generator and bootstrap config exporter."

[[bin]]
name = "onx-genesis"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
